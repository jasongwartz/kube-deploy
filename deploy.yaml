---
application:
  name: kube-deploy
  version: 0.0.1
  remoteType: gcloud
tests:
  - name: Test container can start
    dockerArgs: -v ~/.kube-deploy.conf:/home/kube-deploy/.kube-deploy.conf
    commands:
    - echo $PWD